# Choose an S3 Provider

=== "AWS"
    ```shell
    export S3_PROVIDER=aws
    ```

    :import{aws/auth}

=== "IBM"
    ```shell
    export S3_PROVIDER=ibm
    ```

    :import{ibm/cloud/cos/instance}
